# CLAUDE.md - Sanmu Dashboard Project Memory

## 1. 项目愿景 (Project Vision)
打造一个以数据为核心的个人技术帝国基石——`sanmu_dashboard`。
集成数据采集、高性能处理、AI 分析与可视化大屏。

## 2. 技术栈 (Tech Stack)
- **前端**: Vue 3 + TypeScript + Naive UI + ECharts
- **后端**: FastAPI (Python) + C++ Extensions (Pybind11)
- **数据**: PostgreSQL + TimescaleDB + Redis
- **配置**: YAML 驱动 (GitOps)

## 3. 开发与文档规范 (Standards)
- **文档风格**:
    - 使用 Emoji 🎨 增强标题和列表的可读性。
    - 关键技术名词使用 **加粗**。
    - 复杂逻辑必须通过 **Mermaid** 图表展示。
- **Mermaid 绘图规范**:
    - **配色**: 使用 `classDef` 定义主题色，区分不同模块（如前端绿、后端橙、数据库紫）。
    - **宏观布局**: 使用 `graph TB` (自上而下)。层级之间使用隐藏线 `A ~~~ B` 强制垂直对齐。
    - **微观布局**: 子图(subgraph)内部尽量使用 `direction LR` (左右排列) 以节省纵向空间。
    - **节点**: 文本必须包含在双引号中，支持中文和 Emoji。例: `Node["👤 用户"]`
    - **连线**: 标签紧贴箭头，不留空格。例: `A -->|数据流| B`

## 4. 当前状态 (Current Status)
- **阶段**: 环境搭建 (Environment Setup)。
- **下一步**: 初始化后端 (FastAPI) 与前端 (Vue 3) 骨架。

## 5. 常用命令 (Commands)
*(待补充)*

## 6. MCP 服务器配置 (MCP Server Configuration)

**配置文件位置**: `~/.cursor/mcp.json`

**重要提示**: 
- Cursor 启动 MCP 服务器时使用的环境变量 PATH 可能不包含 Node.js 的安装路径
- **必须使用完整路径**，不能依赖 PATH 环境变量
- 配置完成后需要重启 Cursor 编辑器

### 🔥 Firecrawl MCP Server

**功能定位**: 网页抓取与内容提取工具

**核心功能**:
- **单页/批量网页抓取**: 抓取单个或多个网页的完整内容
- **内容提取**: 支持 Markdown、HTML 等多种格式的内容提取
- **网站映射与爬取**: 发现和爬取整个网站的所有页面
- **结构化数据提取**: 基于 LLM 的智能数据提取，可提取特定结构的信息
- **网页搜索与内容分析**: 网络搜索并分析搜索结果

**可用工具**:
- `firecrawl_scrape` - 抓取单个网页内容
- `firecrawl_batch_scrape` - 批量抓取多个网页
- `firecrawl_map` - 映射网站的所有 URL
- `firecrawl_search` - 网络搜索并提取内容
- `firecrawl_crawl` - 爬取整个网站
- `firecrawl_extract` - 使用 LLM 提取结构化数据

**使用场景**:
- 需要获取网页内容进行分析
- 批量收集网站数据
- 提取特定格式的结构化信息
- 搜索并分析网络内容

**使用建议** ⚠️:
- **谨慎使用，优先考虑 Chrome MCP**: 由于 Firecrawl 是付费服务，会产生 API 调用费用
- **仅在必要时使用**: 当 Chrome MCP 无法满足需求时（如需要批量抓取、网站映射等高级功能）
- **优先使用 Chrome MCP**: 对于单页内容获取、需要登录的网站、浏览器交互等场景，优先使用 Chrome MCP Server
- **成本控制**: 避免频繁调用，特别是批量操作和爬取功能

### 🌐 Chrome MCP Server

**功能定位**: 浏览器自动化与智能控制工具

**核心功能**:
- **直接使用日常 Chrome 浏览器**: 保留登录状态和个性化配置，无需额外设置
- **浏览器自动化**: 跨标签页操作、截图、网络监控等自动化功能
- **智能搜索**: 内置向量数据库，支持语义搜索浏览器内容
- **浏览器管理**: 书签、历史记录、交互操作等完整浏览器控制
- **本地运行**: 保护隐私，所有操作在本地完成

**使用场景**:
- 需要操作浏览器进行自动化任务
- 需要访问需要登录的网站
- 需要截图、监控网络请求
- 需要智能搜索浏览器历史或书签
- 需要让 AI 助手接管浏览器操作

**使用建议** ✅:
- **优先使用，推荐常用**: Chrome MCP Server 是免费的本地服务，可以放心使用
- **网页相关任务首选**: 所有网页浏览、内容获取、浏览器操作等任务优先使用 Chrome MCP
- **适合日常使用**: 可以频繁使用，不会产生额外费用
- **保留登录状态**: 利用其保留登录状态的特性，访问需要认证的网站

### 🔄 两个工具的区别与配合

**核心区别**:
- **Firecrawl MCP**: 专注于网页内容抓取和提取，适合批量获取网页内容
- **Chrome MCP Server**: 专注于浏览器自动化与智能控制，让 AI 助手接管浏览器操作

**配合使用**:
- **优先使用 Chrome MCP**: 对于大多数网页相关任务，优先使用 Chrome MCP Server（免费、本地运行）
- **Firecrawl 作为补充**: 仅在 Chrome MCP 无法满足需求时使用 Firecrawl（如批量抓取、网站映射等高级功能）
- **成本考虑**: 由于 Firecrawl 是付费服务，应谨慎使用，避免不必要的 API 调用

**AI 使用指南**:
- 🟢 **优先使用 Chrome MCP**: 网页浏览、单页内容获取、浏览器操作、需要登录的网站访问等
- 🟡 **谨慎使用 Firecrawl**: 仅在需要批量抓取、网站映射、结构化数据提取等 Chrome MCP 无法完成的高级功能时使用
- 💡 **判断标准**: 如果 Chrome MCP 可以完成的任务，就不要使用 Firecrawl，以节省成本
